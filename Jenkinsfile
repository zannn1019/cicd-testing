pipeline {
    agent any

    stages {
        stage('Fist Step') {
            steps {
                echo 'Hello World'
            }
        }
      stage('list') {
        steps {
          sh 'ls'
          sh 'ls -a'
          sh 'echo list done'
        }
      }
    }
}
